const { Categories } = require('../../src/index');
const { generateSpotifyWebAPI, generateWrapper } = require('../TestFunctions');

test('Placeholder', () => {
    expect({ hello: "hi "}).toStrictEqual({ hello: "hi "});
});