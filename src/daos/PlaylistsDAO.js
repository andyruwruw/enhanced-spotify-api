'use strict';

/**
 * TrackDAO Contructor
 * Creates new Track Data Access Object instance for a given Track.
 * 
 * @param {string} id Spotify ID of the track desired
 * @param {object} data Any additional data to be loaded (optional)
 */
var TrackDAO = function(id, data) {

}

TrackDAO.prototype = {
    getCompleteData: function(spotifyAPI) {

    }
}

module.exports = TrackDAO;